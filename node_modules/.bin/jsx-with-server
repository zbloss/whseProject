#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jsx/bin/jsx-with-server" "$@"
  ret=$?
else 
  node  "$basedir/../jsx/bin/jsx-with-server" "$@"
  ret=$?
fi
exit $ret
